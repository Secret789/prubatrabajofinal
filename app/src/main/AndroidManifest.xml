<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prubatrabajofinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.Usuario.Usuario"></activity>
        <activity android:name=".View.Entrenamiento.Entrenamiento" />
        <activity android:name=".View.Autenticacion.Registrarse" />
        <activity android:name=".View.Autenticacion.Ingresar" />
        <activity android:name=".View.Reproductor.Reproductor" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>